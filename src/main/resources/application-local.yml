spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
  application:
    name: groupware
  datasource:
    url: jdbc:mysql://localhost:3306/groupware?useUnicode=true&serverTimezone=Asia/Seoul
    hikari:
      driver-class-name: com.mysql.cj.jdbc.Driver
      username: groupware
      password: qwe1212
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update    # DDL ?? ?? ?? ??
    properties:
      hibernate:
        show_sql: true # SQL ??
        format_sql: true # ??? ??
        use_sql_comments: true # JPQL ??
  mail:
    host: smtp.gmail.com
    port: 587
    username: valvakim@gmail.com
    password: aapagcbbfkyifaml
    properties:
      mail:
        smtp:
          debug: true
          auth: true
          timeout: 1000
          starttls:
            enable: true

decorator:
  datasource:
    p6spy:
      enable-logging: true

logging.level:
  org.hibernate.type: trace # ?? ???? ??

server:
  tomcat:
    max-http-form-post-size: 10MB

server-url:
  back: http://localhost:8080
  front: http://localhost:3000

init-password-prefix: coveone

secret-key: groupware-backend-java-web-token-secretKey-is-aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa

cloud:
  aws:
    credentials:
      accessKey: AKIATQZCSGQ2GSKKNX5I
      secretKey: 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
    region:
      static: ap-southeast-2
    stack:
      auto: false
    s3:
      bucket: musicfile-bucket

fcm:
  certification: certification-sdk.json